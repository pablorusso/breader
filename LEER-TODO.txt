Este txt esta para que se agreguen las cosas que tenemos que hacer y para que
cada uno tilde lo que hizo.

IMPORTANTE
----------

* exportacion a XMLSchema
* importacion de txt (una boludez) (Damian: lo deje a medias, falta pablo)
* Agregar el id_fav (es el cero) en el archivo4 al comienzo estructura,
  para que este siempre (Damian: medio que parece que está...)
* Soportar reestructurar y ampliar idcat
	Damian - reestructurar:
		* El feedHandler tiene un getUsedFactor que devuelve (reg_usados/reg_totales)
		  no sé cuándo quieren que lo use, o si quieren llamarlo de afuera. La idea
		  sería poner un umbral y que abajo de eso se reestructure
		  Llamada: feedManager->reestructurar();
	Damian - ampliar idcat:  (nota: este ampliar idcat también reestructura)
		* No me acuerdo por qué pero esto no lo podía hacer yo desde adentro...
		  quizás sería bueno que le preguntásemos al Archivo4 cuántas categorías hay
		  tomadas, para poner un umbral de cuántas categorías nos quedan y ahí ampliarla
		  automáticamente.
		  También podríamos pensar en decirle al usuario cuántas categorías va a querer,
		  pero es más sucio.
		  Llamada: feedManager->set_MAX_CAT(NEW_MAX_CAT);
	Damian - ambas anteriores:
		* Obviamente, después de hacer la llamada esa: rezar. ;-)

	Sergio - reestructurar:
		  El cManagerWord tiene un getUsedFactor que devuelve (reg_usados/reg_totales)
		  no sé cuándo quieren que lo use, o si quieren llamarlo de afuera. La idea
		  sería poner un umbral y que abajo de eso se reestructure
		  Llamada: managerWord.reestructurar();

		  Yo opino que se haga desde una opcion en consola xej ./listener -R asi se ejecuta 
		  cuando el programa no esta corriendo y aparte el umbral asi x mas q se lo llame no 
		  siempre se va reestructura.

* Sergio: guardar los archivos en el directorio que te dice General (OK, LISTO)

PRESENTACION LUNES
------------------

* Mirar, modificar o borrar y hacer de nuevo el ppt de la presentación.

BUGS
----

* El RSS de clarin no me anda, porque no me pasa el nombre... ese campo queda vacio y al crear
  el feed tira excepcion por eso
* No me funciona quitar categorias, porque el idFeed pasa vacio y tira error porque no puede
  convertir eso a un numero
* Este feed no trae mensajes? http://rssnewsapps.ziffdavis.com/pcmag.xml

NO TAN IMPORTANTE
-----------------

* Que no se puedan agregar dos categorias con el mismo nombre (Damian: ya esta hecho, a lo bestia)
* Que no se puedan agregar dos feeds con la misma url (elijo url sola) (Damian: ya esta hecho, a lo bestia)
* Configurar los parametros del cArbol y cFileManager a gusto.


CD o DVD incluyendo:
* fuentes de cada uno de los modulos que integran su sistema.
* scripts o makefiles (automake) para poder construir cada modulo. A
  su vez una breve descripcion de como construir cada modulo.
* en el caso de php, incluir una carpeta webroot, donde se encuentre
  organizada toda la interfaz web (incluyendo las bibliotecas que
  utilizan en php) de tal forma que pueda configurarla directamente en
  un vhost apache.

opcionales: (en un directorio separado)
* binarios linkeados estaticamente.
* Bases de prueba generadas para el tp y usadas para testearlo.
* Aplicaciones de test usadas en el desarrollo.
* Set de datos de prueba usado.
* Documentacion en formato digital.
* Todo lo demas que consideren relevante para la entrega.

A su vez se debera entregar una carpeta con la siguiente documentacion:

* Detalle de todas las dependencias necesarias para construir la
  aplicacion (Vital)
* Manual de instalacion y configuracion del entorno de ejecucion.
* Manual de usuario (explicacion del uso de la interfaz web, modulo de
  persistencia y modulo de salida a xml) (Damian: empecé un bosquejo)
* Informacion de contacto al grupo por cualquier consulta que pueda
  surgir.
* Todo lo demas que consideren relevante para la entrega.
